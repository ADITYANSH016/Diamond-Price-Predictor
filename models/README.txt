This folder stores the trained model files:
- mlr_model.pkl    → serialised LinearRegression model
- model_meta.pkl   → metrics dictionary

These are auto-generated when you run:
  python src/model.py
OR when you launch:
  streamlit run app.py  (model trains automatically on first run)
